# .github/workflows/email-step.yml
name: Email Step Action

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3001989@ds.study.iitm.ac.in
        run: echo "Hello, world from email step!"
